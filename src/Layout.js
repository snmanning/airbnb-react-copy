/* eslint-disable */
import React from 'react';
import './Layout.css';

const Layout = (props) => (
    <div className='Layout-container'>
        {props.children}
    </div>
)

export default Layout;